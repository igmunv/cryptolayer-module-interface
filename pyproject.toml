[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cryptolayer-module-interface"
version = "0.0.1"
description = "Base class for CryptoLayer modules"
dependencies = []

[tool.setuptools]
py-modules = ["base_module"]
